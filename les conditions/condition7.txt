// Online C compiler to run C program online
#include <stdio.h>
#include <math.h>
int main() {
      int a ;
      printf("entrer a   : ");
      scanf("%d",&a);
      if(a<=90 && a>=65 ){
         printf("le nombre est alphabet majuscule");
      }else
      printf("le nombre est non alphabet majuscule");
      
    return 0;
}